export type ExpenseItemType = {
  id: string;
  category: string;
  coast: number;
  date: string;
};

export type CategoryItemType = {
  id: string;
  title: string;
  icon: string;
};

export type Category = {
  id: string;
  title: string;
  icon: string;
};
